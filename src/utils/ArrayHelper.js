module.exports = {
    parseStringAsArray(arrayAsString){
        return arrayAsString.split(',').map(arrayAsString => arrayAsString.trim());
    },
    parseArrayAsString(stringAsArray){
        return arrayAsString.split(',').map(arrayAsString => arrayAsString.trim());
    },
    

} 